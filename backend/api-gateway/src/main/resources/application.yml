server:
  port: 8083

spring:
  application:
    name: decky
  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:5173"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: user-auth
          uri: ${USER_AUTH_URI}
          predicates:
            - Path=/auth/**
          filters:
            - AuthenticationFilter

        - id: deck-service
          uri: ${DECK_SERVICE_URI}
          predicates:
            - Path=/api/flashcards/**
          filters:
            - AuthenticationFilter

        - id: today-session-service
          uri: ${TODAY_SESSION_SERVICE_URI}
          predicates:
            - Path=/api/sessions/**
          filters:
            - AuthenticationFilter

jwt:
  secret: ${JWT_SECRET}